- el boton de agregar ejemplo funciona bien.
- necesito que los botones de adelante, atras funcionen.
- necesito coordinar el componente de paginacion con los modales de ejemplos.

const exampleId = 'default-example'; // TODO: reemplaza con el ID correcto si es necesario
const examplePath = `cv-app/examples/${exampleId}/gallery-images`;

actualmente las imagenes se guardan en subnodo de default-example,
cuando haga clic en el boton de adelante, atras, debe cambiar el id de default-example por el siguiente o anterior.

